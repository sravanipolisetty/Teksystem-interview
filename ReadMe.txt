-create a simple java project, copy the source file (src.zip)

-add attached two jar files

-Foo.java -> file has all the code fixes as first part of the exercise. Inline comments are made where ever the code fixes are done.

- com.tek.interview.question.ooad --> this package is part of second exercise. 

--> OrderManager is simple class with original main method to do the testing

--> Junit.java is the JUnit Test framework to test the functionality.

--> this package is created and split the code in better OOAD concepts which will allow better readability and easy maintenance of code.